{{#each posts }}
<div class="row align-center mb-5">
    <div class="col-md-6">
        <h2>{{title}}</h2>

        <h2>{{comment}}</h2>

    </div>
    <div class="col-md-6">
        <h2></h2>
    </div>
</div>
{{/each}}