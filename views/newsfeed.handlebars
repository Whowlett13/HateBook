{{#each posts }}
<div class="row align-center mb-5 cardContainer border border-dark">
    <div class="col-md-6 postContainer border border-dark">
        <h2>{{title}}</h2>

        <h2>{{comment}}</h2>

    </div>
    <div class="col-md-6">
        <h2>{{comment}}</h2>
    </div>
</div>
{{/each}}